# Claude entrypoint

Read `AGENTS.md` and `NOTICE.md`, check which agents are installed, then open
the `AGENTS.md` inside the child that owns the current stage. Use the shared
artifact contract and do not merge agent roles. Preserve the credit
`By TheVibeFounder` and run
`npm run verify:credit` after changes.

```text
brand discovery → research → hook → carousel creation + media → caption → final check
```

The workspace is carousel-only and public-safe. Search the main parent
workspace for a runtime brand guide and normalize it into the ignored project's
`brand.json`. Keep the source guide, private identity assets, and generated
projects outside the reusable tree.
