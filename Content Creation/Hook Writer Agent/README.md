# Hook Writer Agent

Turns validated research into the carousel's cover premise. Its sole output is
`hook.json`; caption and slide production remain downstream responsibilities.

Install this folder directly inside a Content Creation workspace. It requires
workspace API 1.0 and artifact contract 1.0. The workspace discovers
the agent from `agent.json`; the folder name is not used as an API.

By TheVibeFounder
