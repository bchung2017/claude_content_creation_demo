# Caption Writer Agent

Writes the post caption that accompanies a validated carousel. It is not a
video script writer and does not own on-slide copy. It runs after the Carousel
Maker so the caption reflects the slides that were actually produced.

Install this folder directly inside a Content Creation workspace. It requires
workspace API 1.0 and artifact contract 1.0.

By TheVibeFounder
